import { Button } from 'semantic-ui-react';

const HeaderButton = () => {
  return (
    <>
      <Button size="mini">S'inscrire</Button>
      <Button size="mini">Connexion</Button>
      <Button size="mini">Contact</Button>
    </>
  );
};

export default HeaderButton;
